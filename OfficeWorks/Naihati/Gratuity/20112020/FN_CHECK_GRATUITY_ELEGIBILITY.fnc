CREATE OR REPLACE FUNCTION FN_CHECK_GRATUITY_ELEGIBILITY(
P_COMPANYCODE VARCHAR2,
P_DIVISIONCODE VARCHAR2,
P_TOKENNO VARCHAR2
) RETURN  VARCHAR2
AS
LV_RESULT VARCHAR2(1000);
BEGIN
    LV_RESULT := '#SUCCESS';
   -- LV_RESULT := 'STAUS DATE NOT FOUND';
    RETURN LV_RESULT;
END;